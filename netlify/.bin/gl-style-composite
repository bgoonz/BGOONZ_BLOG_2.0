#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@mapbox/mapbox-gl-style-spec/bin/gl-style-composite.js" "$@"
  ret=$?
else 
  node  "$basedir/../@mapbox/mapbox-gl-style-spec/bin/gl-style-composite.js" "$@"
  ret=$?
fi
exit $ret
